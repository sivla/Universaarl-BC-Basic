# Freigegebene Spezifikationen

Dieser Ordner enthaelt nur freigegebene normative Spezifikationen. Blueprint v0.1 wurde am 2026-07-10 bedingt freigegeben und archiviert; seine Requirement- und Scenario-IDs werden aus den Main-Specs als `approved` aufgeloest. Dauerhafte strukturierte Architektur- und Capability-Artefakte liegen an stabilen Pfaden und tragen ebenfalls `approved`. Die Aufloesungsregeln stehen unter `governance/reference-lifecycle.yaml`.
